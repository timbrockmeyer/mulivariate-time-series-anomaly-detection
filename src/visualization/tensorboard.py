
# from torch.utils.tensorboard import SummaryWriter

# stamp = datetime.now().strftime("%Y%m%d-%H%M%S")
# logdir = 'logs/%s' % stamp

# writer = SummaryWriter(logdir)
# writer.add_graph(model, [x[:,:window], *[torch.tensor(np.nan)] * 2])
# writer.close()
